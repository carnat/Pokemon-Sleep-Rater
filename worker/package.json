{
  "name": "pokemon-sleep-rater-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "register-commands": "node scripts/register-commands.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.5.4",
    "vitest": "^4.1.3",
    "wrangler": "^4.80.0"
  }
}
